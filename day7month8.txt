1. 页面有两种或多种展现情况的情况下通过判断缓存数据减少不必要的API请求【如电子面单页面，若开通了电子面单(请求开通状态API)则直接进入电子面单显示页面(请求订单API)，否则就显示需要开通页面】
2. eval()用法
将字符串解析成JSON对象，而其与$.parseJSON和JSON.parse()的区别是，该方式适用于标准或是不标准的字符串(如'{a: 'some', b: 'easy'}'/'{'a':'b', 's':'d'}')

