1. ip绑定域名只表示访问该域名可以访问到对应的服务器并且可以访问上面的内容，目前出现了以下情况：
		a. 绑定了测试环境186但没有发布到测试环境，且本地没有代理到该域名
			结果：您访问的站点现未开放【但是可以看到服务器返回的html，魔筷图标等】
		b. 绑定了测试环境186且发布到测试环境，且本地没有代理到该域名
			结果：可以正常访问
		c. 绑定了测试环境186但没有发布到测试环境，且本地代理到该域名
			结果：可以正常请求该服务器上的接口，可以在本地调试代码