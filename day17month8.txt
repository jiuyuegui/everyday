1. 用fiddle测试的时候记得将所有用到的本地文件替换掉
2. 有时看似相似的两个接口是在不同的服务器上的，故并不是相似的数据字段就是重复多余的
3. 当判断一个东西不存在就选默认值的情况，如a || 5
但当该判断是布尔值的时候，即a || true;只会永远是true
此时需要用判断if(a == "undefined") {
		a = true;
	      }




